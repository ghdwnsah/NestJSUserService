model Client {
  id          String @id @default(uuid())
  name        String?
  clientCode  String  @unique
  isPaid      Boolean @default(false) 
  dbUrl       String?
  dbName      String?
  isDbCreated Boolean @default(false)
  users       User[]  @relation("Clients")
}